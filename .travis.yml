language: node_js
node_js:
  - "6"
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
# notifications:
  # recipients:
    # - utatane.tea@gmail.com
  # email:
    # on_success: change
    # on_failure: always
